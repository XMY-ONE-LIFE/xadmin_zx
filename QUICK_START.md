# 🚀 xadmin 快速启动指南

## 📋 配置信息

### 远程服务器配置

xadmin 使用远程服务器 `10.67.167.53` 上的数据库和缓存服务：

| 服务 | 地址 | 端口 | 密码 |
|------|------|------|------|
| PostgreSQL | 10.67.167.53 | 5433 | amdyes |
| Redis | 10.67.167.53 | 6379 | dsy_201411 |

**注意**: 不需要启动本地的 PostgreSQL 和 Redis 服务！

---

## 🎯 启动步骤

### 终端 1 - 启动后端

```bash
cd /mnt/c/Users/kuntian/xadmin/xadmin
./start_xadmin.sh
```

**启动脚本会自动：**
- ✅ 测试远程数据库连接
- ✅ 检查 Python 依赖
- ✅ 启动 Django 开发服务器

### 终端 2 - 启动前端（新终端）

```bash
cd /mnt/c/Users/kuntian/xadmin/xadmin/web
pnpm dev
```

---

## 🌐 访问地址

| 服务 | 地址 |
|------|------|
| 🎨 前端页面 | http://localhost:5173 |
| 🔧 后端 API | http://127.0.0.1:8000 |
| 📚 API 文档 | http://127.0.0.1:8000/system/docs |
| 📤 YAML 上传 | http://localhost:5173/#/system/testplan-yaml |

---

## 🔑 登录信息

- **用户名**: `admin`
- **密码**: `admin123`

---

## ⚠️ 如果遇到问题

### 1. 远程数据库连接失败

**检查远程服务器状态**:
```bash
# 在远程服务器上执行
docker ps | grep postgres
docker ps | grep redis
```

**测试网络连接**:
```bash
# 在本地 WSL 执行
ping 10.67.167.53

# 测试端口
nc -zv 10.67.167.53 5433  # PostgreSQL
nc -zv 10.67.167.53 6379  # Redis
```

### 2. 端口被占用

```bash
# 杀死占用的进程
kill -9 $(lsof -ti:8000)  # 后端
kill -9 $(lsof -ti:5173)  # 前端
```

### 3. 本地服务冲突

**停止本地服务**（如果不小心启动了）:
```bash
sudo service postgresql stop
sudo service redis-server stop
```

---

## 📝 手动启动（备选方案）

如果启动脚本不工作，可以手动启动：

### 后端
```bash
cd /mnt/c/Users/kuntian/xadmin/xadmin
uv run python manage.py runserver 0.0.0.0:8000
```

### 前端
```bash
cd /mnt/c/Users/kuntian/xadmin/xadmin/web
pnpm dev
```

---

## 🆕 新功能: 测试计划 YAML 上传

1. 登录系统: http://localhost:5173
2. 访问 YAML 上传页面: http://localhost:5173/#/system/testplan-yaml
3. 上传 `.yaml` 文件进行分析

详细文档: `TESTPLAN_YAML_README.md`

---

## 🔧 配置文件位置

- **数据库配置**: `xadmin/settings.py` (第 88-100 行)
- **Redis 配置**: `xadmin/settings.py` (第 103-118 行)

---

**创建日期**: 2025-11-11  
**最后更新**: 2025-11-11







